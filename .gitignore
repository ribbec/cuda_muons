
__pycache__/

.DS_Store

geant4/cpp/build/*
geant4/cpp/externals/*
faster_muons_torch/build/*
container_install_cuda.sh
data/muons/*.pkl
data/muons/*.npy
data/muons/*.h5
data/outputs/*
data/magnet_params.csv
data/*.pkl
!data/alias_histograms*.pkl
data/*.npy
data/*.h5
experiments/plots/*
*.egg-info

# Build outputs: the container image and the built wheel are large and
# regenerable (cuda12.8-build.def / install_cuda.sh). The .def itself is tracked.
dist/*.sif
dist/*.whl

# Fitted beam models are inputs, not outputs: keep them tracked despite data/*.pkl.
!data/gmm/*.pkl

# setuptools build tree
build/
